# sql_walmart
Example MySQL

# Virtual environment
## Create venv
```bash
python3 -m venv .venv
source .venv/bin/activate
```

## Create requirements.txt
```bash
pip freeze > requirements.txt
```

## Install
```bash
pip install -r requirements.txt
```

# Install libraries (packages) using pip
```bash
pip install mysql-connector-python
# add more packages
#...
```

Note: Automatically add libraries installed by pip from the terminal
```bash
pip install -r requirements.txt
# See file 
# mysql-connector-python==9.5.0
# ...
```
fdfd 

dfdf

gfgf